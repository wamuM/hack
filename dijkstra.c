#include "dijkstra.h"

/* 
 * COSAS POR HACER
 *
 * - Definir el struct Path
 */

Path dijkstra(graph* g, int start, int goal) {
  // Here is the space for implementing the dijkstra algorithm
}

void dijkstra_free_path(Path &path) {
  // Here we free all the memory allocated inside Path
}
